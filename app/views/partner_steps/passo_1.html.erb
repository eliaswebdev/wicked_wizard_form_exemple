<h3 class="text-success">Passo 1 <%= @partner.id %></h3>
<%= bootstrap_form_for @partner, url: wizard_path, method: :put do |f| %>
  <%= f.text_field :name %>

  <%= render 'partner_steps/controls', f: f %>
<% end %>